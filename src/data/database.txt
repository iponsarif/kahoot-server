ERD: https://www.lucidchart.com/invitations/accept/db3742d1-e839-4bf1-95c4-d5aa99800181

CREATE TABLE users(
id SERIAL PRIMARY KEY,
username VARCHAR(100) UNIQUE NOT NULL,    
password VARCHAR(100) NOT NULL,
fullname VARCHAR(200) NOT NULL,
email VARCHAR(100) UNIQUE NOT NULL,
created_at TIMESTAMP(6) WITH TIME ZONE,
modified_at TIMESTAMP(6) WITH TIME ZONE,
deleted_at TIMESTAMP(6) WITH TIME ZONE
);

CREATE TABLE quizzess(
id SERIAL PRIMARY KEY,
creator_id INTEGER NOT NULL,
title VARCHAR(300) NOT NULL,
category VARCHAR(100) NOT NULL,
created_at TIMESTAMP(6) WITH TIME ZONE,
modified_at TIMESTAMP(6) WITH TIME ZONE,
deleted_at TIMESTAMP(6) WITH TIME ZONE,
FOREIGN KEY (creator_id) REFERENCES users (id)
);

CREATE TABLE questions(
id SERIAL PRIMARY KEY,
quiz_id INTEGER NOT NULL,
question VARCHAR(300) NOT NULL,
number INTEGER NOT NULL,
answer VARCHAR(50) NOT NULL,
created_at TIMESTAMP(6) WITH TIME ZONE,
modified_at TIMESTAMP(6) WITH TIME ZONE,
deleted_at TIMESTAMP(6) WITH TIME ZONE,
FOREIGN KEY (quiz_id) REFERENCES quizzess (id)
);

CREATE TABLE options(
id SERIAL PRIMARY KEY,
question_id INTEGER NOT NULL,
option VARCHAR(20) NOT NULL,
detail VARCHAR(100),
created_at TIMESTAMP(6) WITH TIME ZONE,
modified_at TIMESTAMP(6) WITH TIME ZONE,
deleted_at TIMESTAMP(6) WITH TIME ZONE,
FOREIGN KEY (question_id) REFERENCES questions (id)
);

CREATE TABLE games(
game_pin INTEGER PRIMARY KEY,
quiz_id INTEGER NOT NULL,
created_at TIMESTAMP(6) WITH TIME ZONE,
modified_at TIMESTAMP(6) WITH TIME ZONE,
deleted_at TIMESTAMP(6) WITH TIME ZONE,
FOREIGN KEY (quiz_id) REFERENCES quizzess (id)
);

CREATE TABLE leaderboards(
game_pin INTEGER NOT NULL,
participant VARCHAR(20) NOT NULL,
score INTEGER,
FOREIGN KEY (game_pin) REFERENCES games (game_pin)
);